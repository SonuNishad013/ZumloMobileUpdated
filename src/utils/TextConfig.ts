import { moderateScale, textScale } from "../constant/responsiveStyle";

export const TextConfig = {
  AIChatmessageLimit: 10,
};

export const maxLength = {
  maxLengthForPhoneNumber: 10,
};

export const dashboardLabelAlignemnt = {
  leftSpace: moderateScale(15),
  topSpace: moderateScale(25),
  bottomSpace: moderateScale(10),
  fontSize: textScale(14),
};

export const textLabelSize = {
  headerTextSize: textScale(28),
  screenHeader: textScale(24),
  fieldText: textScale(20),
  mainTitle: textScale(18),
  titleFont: textScale(16),
  subHeaderTextSize: textScale(14),
  subtTitleFont: textScale(12),
  smallTitleFont: textScale(10),
};
